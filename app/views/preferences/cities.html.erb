<%= session[:city] %>

<%# insert the search bar %>

<div class="container">
  <%= simple_form_for :city, url: choose_duration_path, method: :get, class: "d-flex" do |f| %>
  <%= f.input :city, collection: City.all %>
  <%= f.button :button do %>
    <% "OK" %>
    <% end %>
  <% end %>
</div>
